import { FilterApartPipe } from './filter-apart.pipe';

describe('FilterApartPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterApartPipe();
    expect(pipe).toBeTruthy();
  });
});
