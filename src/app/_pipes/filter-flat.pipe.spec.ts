import { FilterFlatPipe } from './filter-flat.pipe';

describe('FilterFlatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFlatPipe();
    expect(pipe).toBeTruthy();
  });
});
